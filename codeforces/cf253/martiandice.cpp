#include <iostream>
#include <vector>
#include <cstdio> 
#include <algorithm>
#include <set>
#include <string>
#include <map>
#include <queue>
#include <iomanip>
#include <cmath>
#include <complex> 

using namespace std;

#define REP(i, to) for(int i=0; i<to; i++)

typedef long long int LLI;
typedef pair<int, int> PII; 

//ch, co, dr, hu, ta, empty
double P[21][21][21][21][21][21]; 


int main(){
  REP(i, 21) REP(j, 21) REP(k, 21) REP(m, 21) REP(q, 21) REP(r, 21) P[i][j][k][m][q][r] = 0;   

  return 0; 
}
