#include <iostream>
#include <set>
#include <map>
#include <algorithm>
#include <vector>

using namespace std;

#define REP(i, to) for(int i=0; i<to; ++i)

int main() {
  int T;
  cin >> T;
  REP(t, T) {
    cout << "Case #" << t+1 << ": ";
  }
  return 0;
}
